# =============================================================================
# Copy to .env and fill in your values.
# LOCAL: Use DATABASE_PUBLIC_URL (or DB_*). Leave DATABASE_URL unset.
# RAILWAY: Set DATABASE_URL (and optionally PGDATABASE, PGHOST, etc.) in Variables.
# =============================================================================

# Public DB URL (for local dev - use the "Public network" URL from Railway Postgres)
DATABASE_PUBLIC_URL=postgresql://postgres:PASSWORD@HOST.proxy.rlwy.net:PORT/railway

# For Railway deploy only - set in Railway Variables (internal URL for same project)
# DATABASE_URL=postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway

# Optional: separate DB variables (for local dev if you prefer)
# DB_HOST=HOST.proxy.rlwy.net
# DB_PORT=56539
# DB_NAME=railway
# DB_USER=postgres
# DB_PASSWORD=your_password

# Railway also provides (optional - app uses DATABASE_URL / DATABASE_PUBLIC_URL):
# PGDATABASE=railway
# PGHOST=postgres.railway.internal
# PGPASSWORD=your_password
# PGPORT=5432
# PGUSER=postgres

# JWT Secret (change in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server
PORT=5000
NODE_ENV=development

# CORS: comma-separated origins (e.g. https://your-app.vercel.app,http://localhost:3000)
FRONTEND_URL=http://localhost:3000

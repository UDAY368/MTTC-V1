# =============================================================================
# Copy to .env and fill in your values.
# LOCAL (testing against Railway): Set DATABASE_PUBLIC_URL → migrations & app use Railway DB.
# RAILWAY: Set only DATABASE_URL (internal URL) in Variables; DATABASE_PUBLIC_URL is not set.
# =============================================================================

# Public DB URL (local testing → Railway Postgres). If set, it overrides DATABASE_URL for migrations & app.
DATABASE_PUBLIC_URL=postgresql://postgres:PASSWORD@HOST.proxy.rlwy.net:PORT/railway

# For Railway deploy only - set in Railway Variables (internal URL). Ignored locally if DATABASE_PUBLIC_URL is set.
# DATABASE_URL=postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway

# Optional: separate DB variables (for local dev if you prefer)
# DB_HOST=HOST.proxy.rlwy.net
# DB_PORT=56539
# DB_NAME=railway
# DB_USER=postgres
# DB_PASSWORD=your_password

# Railway also provides (optional - app uses DATABASE_URL / DATABASE_PUBLIC_URL):
# PGDATABASE=railway
# PGHOST=postgres.railway.internal
# PGPASSWORD=your_password
# PGPORT=5432
# PGUSER=postgres

# JWT Secret (change in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server
PORT=5000
NODE_ENV=development

# CORS: comma-separated origins. Use production URL when frontend is deployed.
# Local dev: http://localhost:3000
FRONTEND_URL=https://meditationteachertraining.vercel.app

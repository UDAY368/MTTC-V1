# Backend API URL (no trailing slash required; trailing slashes are normalized)
# Local dev: leave unset or use http://localhost:5000
# Production: your deployed backend (e.g. Railway)
NEXT_PUBLIC_API_URL=https://meditation-training-be.up.railway.app

# Default public course ID for the landing "About Course" CTA. When set, the button works immediately
# without waiting for /api/public/courses — recommended in production to avoid latency under high traffic.
# Example: NEXT_PUBLIC_DEFAULT_COURSE_ID=cmkzk0jms000i6zff7no73813
NEXT_PUBLIC_DEFAULT_COURSE_ID=
